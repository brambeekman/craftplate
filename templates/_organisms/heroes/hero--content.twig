{%- import '_macros/atoms' as render -%}

{%- set utilities = utilities ?? '' -%}

{%- minify -%}

    <div>
        <h2 class="font-bold text-2xl md:text-5xl leading-tight mb-4">
            {{- render.atom('text', 'title', { content: content.heading ?? entry.title }) -}}
        </h2>

        {%- if content.description -%}
            <div class="prose md:text-xl">
                {{- render.atom('text', 'content', { content: content.description }) -}}
            </div>
        {%- endif -%}

        {%- if content.buttons -%}

            {{- render.atom('button', 'group', {

                buttons: content.buttons,
                type: 'primary',
                utilities: 'mt-6 space-x-4',
                gaCategory: 'hero-cta',
                gaAction: 'click',
                inverted: false,

            }) -}}

        {%- endif -%}
    </div>

{%- endminify -%}